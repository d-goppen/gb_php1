/* 1. Установить программное обеспечение:
   - веб-сервер
   - базу данных
   - интерпретатор
   - текстовый редактор.
   Проверить, что все работает правильно.
*/
// На данный момент используется Open Server 5.2.2 / php_7.3-x64


/* 2. Выполнить примеры из методички и разобраться, как это работает.
*/
// Выполнили на уроке и посмотрел дополнительно. Всё очень похоже на Java и JavaScript.


/* 3. Объяснить, как работает данный код:
*/
// Объяснение к каждой строке записаны в комментарии перед строкой.
<?php
    // Присваиваем переменной $a значение 5 (тип - целое)
    $a = 5;
	// Присваиваем переменной $b значение 05 (тип - строка)
    $b = '05';
	// Проводим "нестрогое" сравнение, при котором интерпретатор
    // приводит тип $b к числу: '05' => 5 (лидирующий 0 отбрасывается).
    var_dump($a == $b);         // Почему true?
	// Используем явное преобразование строки к целому числу: '012345' => 12345 (см. выше)
    var_dump((int)'012345');     // Почему 12345?
	// Проводим "строгое" сравнение переменных разных типов.
    // Т.к. типы разные, то переменные не могут быть равны.
    var_dump((float)123.0 === (int)123.0); // Почему false?
	// Явно приводим типы к целому числу.
	// При преобразовании строки в число берётся начало строки, до первого не числового символа.
	// В данном случае получаем пустую строку, которая в php равна 0.
    var_dump((int)0 === (int)'hello, world'); // Почему true?
?>

/* 4. Используя имеющийся HTML-шаблон, сделать так,
      чтобы главная страница генерировалась через PHP.
      Создать блок переменных в начале страницы.
      Сделать так, чтобы h1, title и текущий год генерировались
      в блоке контента из созданных переменных.
*/
// Решение см в файле index.php


/* 5* Используя только две целочисленные переменные,
      поменяйте их значение местами.
      Например, если a = 1, b = 2, надо, чтобы получилось b = 1, a = 2.
      Дополнительные переменные использовать нельзя.
*/
// Классическая задача для любого языка )))
// Диапазон случайных чисел задан для примера. Работет с любыми числами.
<?php
  $a = mt_rand(0, 999);
  $b = mt_rand(0, 999);
  $a = $a ^ $b;
  $b = $b ^ $a;
  $a = $a ^ $b;
?>
